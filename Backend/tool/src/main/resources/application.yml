spring:
  cache:
    type: redis
    redis:
      time-to-live: 10m
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/dfd_builder}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Ganesh@24}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:ganeshkolare5@gmail.com}
    password: ${MAIL_PASSWORD:dlvpohcjjtvhwzfc}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: ${JWT_EXPIRATION:86400000}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

